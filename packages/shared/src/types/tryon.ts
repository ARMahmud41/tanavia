export interface TryOnSettings {
  enabled: boolean;
  modelHeight: string;
  modelSkin: string;
  modelHair: string;
  stageBg: string;
  defaultScale: number;
  defaultY: number;
  minScale: number;
  maxScale: number;
  minY: number;
  maxY: number;
  note: string;
}